# Core runtime dependencies (mirrors pyproject.toml [project.dependencies]).
#
# Prefer installing the package with optional extras instead of this file alone:
#   pip install -e ".[all]"          # full dev / notebook / Bayesian stack
#   pip install -e ".[workflows]"    # notebooks only
#   uv sync --extra all              # same, using uv
#
# Legacy one-liner (core only):
#   pip install -r requirements.txt && pip install -e .

pydantic>=2.11.0
pydantic-xml>=2.17.0
lxml>=5.3.0
numpy>=2.2.0
pandas>=2.2.0
requests>=2.32.0
marimo>=0.23.10
anthropic>=0.111.0
sympy>=1.13
