"""
Bayesian inference on top of analysis outputs (e.g. DataFrames from ``extract_property_dataframe``).

Add optional dependencies via ``pip install fairfluids[bayesian]`` (see ``pyproject.toml``)
and implement model fitting here (e.g. PyMC / ArviZ) without pulling them into the core install.
"""

from __future__ import annotations

__all__: list[str] = []
