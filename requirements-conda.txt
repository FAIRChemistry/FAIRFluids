# Core dependencies (some may need to be installed via pip in conda)
pydantic-xml>=2.0.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
isort>=5.0.0
flake8>=5.0.0
mypy>=1.0.0

# Documentation
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0
myst-parser>=0.18.0

# Scientific computing (if not available in conda)
# rdkit-pypi>=2022.9.1

# Install the package in development mode
-e .
