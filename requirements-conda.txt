# Pip-only packages for a custom minimal conda environment.
#
# NOT needed after `conda env create -f environment.yml` — that file already
# installs these packages and the project in editable mode (`-e .`).
#
# Use this only when you conda-installed core/scientific/Jupyter packages
# manually and still need the pip-only stack from pyproject.toml.

pydantic-xml>=2.17.0
anthropic>=0.111.0
marimo>=0.23.10
neo4j>=5.14.0
openpyxl>=3.1.0
plotly>=5.17.0
flask>=2.3.0
pint>=0.25.0
numpyro>=0.20.0
jax>=0.7.0
jaxlib>=0.7.0
arviz>=0.22.0
arviz-plots>=1.0.0
arviz-base>=1.0.0
arviz-stats>=1.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
-e .
